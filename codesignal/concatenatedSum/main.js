const concatenatedSum = (a) => {
  const start = a[0].toString() + a[0].toString();
  const end = a[a.length - 1].toString() + a[a.length - 1].toString();
  const result = [parseInt(start), parseInt(end)];
  for (let i = 0; i < a.length)
}

concatenatedSum([1, 2, 3])
